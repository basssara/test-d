export enum AccessRoles {
  ADMIN = 1,
  OPERATOR = 2,
  DOCUMENT = 3,
  ID_CARD = 4,
  TEMPORARY_PERMAMENT_REGISTRATION = 5,
}

export enum AccessRolesInDB {
  ADMIN = 'admin',
  OPERATOR = 'operator',
  DOCUMENT = 'document',
  ID_CARD = 'id_card',
  TEMPORARY_PERMAMENT_REGISTRATION = 'temporary_permission_registration',
}
