export enum Status {
  ACCEPTED = 'accepted',
  PAYMENT_ACCEPTED = 'payment',
  PROCEEDING = 'proceeding',
  PERSONALTIZATION_SENT = 'personalization_sent',
  PENDING = 'pending',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
}
